# オリジナル問題 TS-01【Drill / 目安7〜10分】

## 対象領域
トラブルシューティング（クラスタコンポーネント／コンテナ出力ストリーム・Events の評価／サービスとネットワーク）

---

## 背景

あなたは、社内の在庫管理APIを運用するプラットフォームチームの担当者である。
先ほど、このAPIを新しい設定内容で再デプロイしたところ、再デプロイ後から当該APIが他サービスへ応答を返さなくなった。
利用部門から「在庫画面が表示されない」との連絡が入っている。

アプリケーションのコンテナイメージおよびアプリケーションコードは、再デプロイの前後で変更していない。

---

## 初期観測情報（Initial Observation）

調査開始時点で、次の事実が確認できている。これは原因ではなく、調査の入口である。

- 対象APIの Pod は STATUS が `Running` だが、`READY` が `0/1` のまま変化しない。

---

## 要件

- 対象APIの Pod が正常に `Ready`（`1/1`）になること。
- 対象APIの Service が、その Pod へトラフィックを届けられる状態に復旧すること。

---

## 前提条件

- 対象APIは、1つの Deployment と1つの Service で構成されている。
- `kubectl` は利用可能な状態にある。
- 今回の事象は、直前の再デプロイ以降に発生している。

---

## 制約

- アプリケーションのコンテナイメージおよびアプリケーションコードは変更しないこと。
- レプリカ数は維持すること。
- 症状を見かけ上だけ消すのではなく、根本原因を修正すること。
- 特定した原因と実施した修正を、根拠（確認したリソースの状態やログ）とともに説明できること。

---

## 完了条件

- 対象APIの Pod が `READY 1/1` になっている。
- 対象 Service の Endpoints に当該 Pod のアドレスが登録されている。
- 事象の「原因」と「実施した修正」を根拠とともに説明できる。
