# オリジナル問題 HPA【Drill / 目安7〜10分】

## 対象領域
ワークロードとスケジューリング（ワークロードの自動スケーリングを構成する）

## 教材種別
Drill（試験対策問題・短時間・調査/設計対象を絞る）

---

## 背景

あなたは、あるECサイトを運用するプラットフォームチームの担当者である。
まもなく大型セールが始まり、商品検索を担うバックエンドAPIへのアクセスが、平常時の数倍まで急増すると見込まれている。

このAPIは現在、レプリカ数を固定して稼働している。
先日の負荷試験では、アクセス増加に伴い応答が遅延したが、レプリカ数は固定のままで増えなかった。
運用チームは、負荷に応じてレプリカ数が自動で増減する構成に変更したいと考えている。

---

## 初期観測情報（Initial Observation）

調査開始時点で、次の事実が確認できている。これは原因ではなく、設計を始めるための入口である。

- 負荷試験中、対象APIの Pod のCPU使用率はピーク時に大きく上昇したが、レプリカ数は固定のまま変化しなかった。

---

## 要件

- 対象APIが、負荷に応じてレプリカ数を自動で増減できるようにすること。
- 平常時は少ないレプリカで稼働し、負荷上昇時には上限まで拡張できること。
- スケールの基準として、Pod のリソース使用状況（CPU）を用いること。
- 急激な負荷減少があっても、レプリカ数が過度に急縮小して処理中のリクエストへ影響が出ないよう配慮すること。

---

## 前提条件

- 対象APIは、1つの Deployment として稼働している。
- 対象 Deployment のコンテナには、リソース要求（request）が設定されている。
- クラスタでは、Pod のリソース使用状況を取得できる仕組みが動作している。
- `kubectl` は利用可能な状態にある。

---

## 制約

- Deployment 本体（コンテナイメージ・アプリケーションコード）は変更しないこと。
- レプリカ数を手動で固定し続ける運用は行わないこと（自動スケーリングに委ねる）。
- コスト観点から、レプリカ数には上限を設けること。
- 設定した各値について「なぜその値・その方針にしたのか」を説明できること。

---

## 完了条件

- 対象 Deployment を参照する自動スケーリングの設定が作成されている。
- 負荷に応じて、レプリカ数が設定した最小〜最大の範囲で増減し得る状態になっている。
- 現在のメトリクスと、それに対するレプリカ数の状況を確認できる。
- 設定内容（スケール対象・基準・レプリカ範囲・縮小時の挙動）の設計意図を説明できる。
