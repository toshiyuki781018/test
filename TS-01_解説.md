# オリジナル問題 TS-01 解説【講師用教材】

> 本ファイルは講師用。答えそのものより、**受講者がどのような思考順序で調査・判断・修正・確認に到達するか**を口頭補足しやすい形で整理している。

---

## この問題で身につく能力

- 「Pod が `Running` でも `Ready` とは限らない」というコンテナのライフサイクル理解。
- 症状（Pod が Ready にならない）から、`describe` の Events → Logs へと**確認対象を段階的に絞り込む**トラブルシューティングの型。
- Readiness の状態が Service の Endpoints 登録に直結するという、**Pod と Service のつながり**の理解。
- ログ・イベントという「コンテナ出力ストリーム」を根拠として原因を説明する力。

---

## Exam Curriculum との対応

- トラブルシューティング（30%）
  - クラスタコンポーネントのトラブルシューティング
  - コンテナ出力ストリームの管理と評価
  - サービスとネットワークのトラブルシューティング
- （関連）ワークロード：堅牢な自己修復アプリケーションのプリミティブ（Readiness/Liveness）の理解

---

## 考えるポイント

### 受講者が最初に何を疑うべきか
- Pod は `Running`＝スケジュール自体は成功している。つまり「配置」ではなく「**コンテナが正常と判定されていない**」側に目を向ける。
- `READY 0/1` は Readiness の未達を意味する。ここで「なぜ Ready にならないのか」を Pod の内部状態から確認する、と方向づけできるかが分かれ目。

### なぜそこを見るのか
- `Running` かつ `0/1` の組み合わせは、スケジューリング障害（Pending）でもクラッシュ（CrashLoopBackOff）でもない。この切り分けにより、Node やスケジューラ層を調べる必要がないと判断でき、**調査範囲を1〜2段階に絞れる**。
- Ready 判定はコンテナの状態評価の結果なので、その根拠は Pod の Events とコンテナのログに現れる。だから次に見るのは `describe` と `logs`。

---

## 調査の流れ

```
現象（Running だが 0/1 READY）
        ↓
kubectl get pods で状態を再確認（Pending / CrashLoop でないことを確認 → 配置は成功）
        ↓
kubectl describe pod（Events を読む：Ready になれない理由の手がかり）
        ↓
kubectl logs（コンテナ自身が何を出力しているか＝一次情報）
        ↓
原因特定（再デプロイで入った設定と、観測された挙動の食い違いを突き合わせる）
        ↓
修正（Deployment 側の該当設定を正す）
        ↓
確認（Pod が 1/1 になる → Service の Endpoints に登録される）
```

> 想定される代表的な原因は、再デプロイ時に入った Readiness 設定と実際のコンテナの応答先の不一致（例：応答を確認するポートやパスの指定ずれ）。**ただし受講者には答えを渡さず、Events とログという根拠から自力で結論させる**のが本問の狙い。

---

## 実施内容（代表的なコマンドと、なぜ実行するのか）

| 手順 | コマンド例 | なぜ実行するのか |
|---|---|---|
| 状態の再確認 | `kubectl get pods -n <ns> -o wide` | `Running`＋`0/1` を確定し、Pending/CrashLoop でないと切り分けるため。ここで調査範囲を決める。 |
| 原因の手がかり | `kubectl describe pod <pod> -n <ns>` | Events に Ready になれない理由（プローブ失敗など）が時系列で出る。**推測でなく記録から**判断するため。 |
| 一次情報の確認 | `kubectl logs <pod> -n <ns>` | コンテナ自身の出力を確認し、Events の手がかりを裏づける／反証するため。 |
| 設定の突き合わせ | `kubectl get deploy <deploy> -n <ns> -o yaml` | 再デプロイで入った設定値と、観測された挙動のズレを特定するため。 |
| 修正 | `kubectl edit deploy <deploy> -n <ns>` | 根本原因（設定のズレ）を正す。イメージやコードには触れない。 |

> コマンドの丸暗記ではなく、「`get` で範囲を決め、`describe` で根拠を得て、`logs` で裏を取る」という**目的の連鎖**として教えること。

---

## 確認方法

- `kubectl get pods -n <ns>` … 対象 Pod が `READY 1/1` になっていること。
- `kubectl get endpoints <service> -n <ns>`（または `kubectl get endpointslices`）… Service の Endpoints に Pod の IP が登録されたこと。Ready になると自動で登録される流れを体感させる。
- 可能なら同一クラスタ内から Service 経由でアクセスし、応答が返ることを確認。
- 受講者に「何を根拠に直ったと言えるか」を口頭で説明させると理解が定着する。

---

## 学習ポイント

- **Running ≠ Ready**：スケジュール成功と、トラフィックを受けられる状態は別物。Readiness はその橋渡し。
- **Ready と Endpoints は連動する**：Ready でない Pod は Service の対象から自動的に外れる。だから「一部応答しない」は Readiness を疑う定石になる。
- **調査は層で絞る**：`Running/0/1` という一点から、Node・スケジューラ層を除外できる。症状の組み合わせで調査範囲を決めるのがトラブルシューティングの効率化。
- **根拠で語る**：Events とログという記録に基づいて原因を説明する習慣をつける。運用では「直った」より「なぜ直ったか」が引き継ぎ可能な資産になる。
