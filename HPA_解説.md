# HPA問題 解説

## この問題では何を考えればよかったのか

この問題では、ECサイトの商品検索APIに対して、アクセス状況に応じてPod数を自動で増減できるようにすることが目的でした。

ここで重要なのは、

**「HPAを作ること」**

ではなく、

**「どのような要件をKubernetesの設定へ落とし込むか」**

という考え方です。

---

# まず、問題文を整理してみましょう

問題文には、次のような要件が書かれていました。

| 問題文                 | 考えること         |
| ------------------- | ------------- |
| Deploymentを自動スケールする | 何を対象にするのか     |
| CPU使用率を基準にする        | 何を見て増減するのか    |
| 最小・最大レプリカ数          | どこまで増減するのか    |
| 急激なスケールダウンを避ける      | どのように増減させるのか  |
| 利用可能なAPIを確認する       | 実行環境で利用できる設定か |

このように、

**要件を一つずつ整理すると、何を設定しなければならないかが見えてきます。**

---

# 次に、設計の順番を考えます

HPAを設計するときは、次の順番で考えると整理しやすくなります。

```text
要件を確認する
        ↓
何をスケールするのか
        ↓
何を基準にスケールするのか
        ↓
どこまでスケールするのか
        ↓
どのようなスケーリング挙動を求めるのか
        ↓
利用可能なAPIを確認する
        ↓
設定を適用する
        ↓
期待した動作になっているか確認する
```

重要なのは、

**いきなりYAMLを書き始めないこと**です。

まずは要件を整理し、必要な設定を考えることが大切です。

---

# Kubernetesではどの設定になるのか

整理した要件は、Kubernetesでは次のような設定になります。

| 要件               | Kubernetesで設定する項目 |
| ---------------- | ----------------- |
| Deploymentを対象にする | scaleTargetRef    |
| CPU使用率を基準にする     | metrics           |
| 最小レプリカ数          | minReplicas       |
| 最大レプリカ数          | maxReplicas       |
| 急激なスケールダウンを避ける   | behavior          |
| 利用可能なAPIを確認する    | apiVersion の確認    |

ここで覚えてほしいのは、

**設定項目そのものではなく、**

**「要件がどの設定へ変換されるのか」**

という対応関係です。

---

# この問題の模範実装

ここまで整理すると、問題で求められているHPAは次のような構成になります。

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ec-api-hpa
  namespace: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ec-api

  minReplicas: 2
  maxReplicas: 6

  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60

  behavior:
    scaleDown:
      stabilizationWindowSeconds: 30
```

重要なのは、このYAMLを暗記することではありません。

ここまで整理した

**「要件 → 設計 → Kubernetes設定」**

という流れを理解した結果、このYAMLになるということです。

---

# 作成後に確認すること

HPAは作成しただけでは終わりではありません。

次の点を確認しましょう。

* HPAが正常に作成されているか
* 対象Deploymentを参照しているか
* CPU使用率が取得できているか
* 現在値と目標値が正しく表示されているか
* レプリカ数が最小・最大の範囲で制御される状態になっているか

設定だけでなく、

**期待した動作になっていることを確認する**

ことも重要な作業です。

---

# この問題で学んでほしいこと

今回学んでほしいのは、

HPAの設定方法ではありません。

学んでほしいのは、

```text
要件を理解する

↓

設計する

↓

Kubernetesの設定へ翻訳する

↓

実装する

↓

動作を確認する
```

という考え方です。

試験では、設定値や条件が変わることがあります。

しかし、

* 何を対象にするのか
* 何を基準にするのか
* どこまで増減するのか
* どのような挙動を求めるのか

という設計の順番は変わりません。

この「判断の型」を身につけることで、初めて見る問題や実務の課題に対しても、自分で考えながらHPAを構成できるようになります。
