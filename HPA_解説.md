# オリジナル問題 HPA 解説【講師用教材】

> 本ファイルは「この問題の正解（完成YAML・設定値）」を示すものではない。
> 受講者が **HPA を設計するときの考え方（判断の型）** を身につけるための教材である。
> 講師は、答えを渡すのではなく、判断の順序を一緒にたどる進行を想定すること。

---

## 1. この問題で身につく能力

Exam Curriculum「ワークロードとスケジューリング（15%）」の次の能力を確認する。

- ワークロードの自動スケーリングを構成する力
- 「なぜスケールが必要か」という要件を、Kubernetes の仕組み（スケール対象・メトリクス・レプリカ範囲・挙動）に翻訳する設計力
- スケーリングが前提とする条件（メトリクスの取得、リソース要求の設定）を理解しているか

覚えるべきは特定の設定値ではなく、**「初めて出会うスケーリング要件でも、同じ順序で設計を組み立てられる型」** である。

---

## 2. 判断の流れ

HPA は「何を・何を基準に・どこまで・どう動かすか」を順に決めていく。この順序を体で覚えさせる。

```
① 何をスケール対象にするのか
     → スケールさせたいワークロード（Deployment 等）を特定する
        ↓
② 何を基準にスケールするのか
     → 負荷を表す指標を決める（本問では CPU 使用状況）
     → その指標が「何に対する割合か」を意識する
        ↓
③ 最小・最大レプリカをどう考えるのか
     → 平常時に必要な最低限（可用性）＝最小
     → コスト・クラスタ容量の上限（守るべき天井）＝最大
        ↓
④ スケーリング挙動をどう設計するのか
     → 増やすときと減らすときで求められる性質は違う
     → 特に「急な縮小」で稼働中の処理へ影響が出ないよう配慮するか
        ↓
⑤ 最後にクラスタで利用可能な形を確認する
     → その環境で使える API の形・メトリクス取得の可否を確認して確定する
```

この5段階のうち、受講者がつまずきやすいのは②の「割合の基準」と④の「増減で性質が違う」点。ここを重点的に補足する。

---

## 3. 考えるポイント

### 受講者が最初に確認すべきこと・その理由
- **スケール対象は本当にそのワークロードか**：HPA はレプリカを持つワークロードを対象にする。まず「誰を伸縮させるのか」を確定しないと、以降の設計が始まらない。
- **基準にする指標の意味**：CPU 使用率でスケールする場合、それは「コンテナのリソース要求（request）に対する割合」で評価される。だから *request が設定されていること* が前提になる。ここを飛ばすと「メトリクスが取れずスケールしない」に直結する。→ 受講者に「なぜ前提条件に request の話があるのか？」を考えさせる。
- **メトリクスが取得できる状態か**：使用状況を測る仕組みが動いていなければ、そもそも判断材料が得られない。「設定したのに動かない」ときの最初の疑いどころ。

### なぜその順番なのか
- 対象 → 基準 → 範囲 → 挙動、の順は「まず動かす相手を決め、次に何を見て動かすかを決め、最後にどこまで・どう動かすかを絞る」という**依存関係の順**になっているから。逆順では設計が破綻しやすい。

---

## 4. Kubernetes が提供する確認手段

コマンドの暗記が目的ではない。**なぜその情報を見るのか**をセットで説明する。

- 対象ワークロードの状態・リソース要求の確認（例：`kubectl get deploy -o yaml` / `kubectl describe deploy`）
  → CPU 割合でスケールする前提（request の有無）を確認するため。
- 利用可能な API 形式の確認（例：`kubectl api-resources` / `kubectl explain`）
  → その環境で使える自動スケーリングの形を確認し、想定と実環境のズレを避けるため。「テンプレート通りに書いたのに適用できない」を防ぐ。
- 現在のメトリクスとスケール状況の確認（例：`kubectl get hpa` / `kubectl describe hpa`）
  → 現在値／目標値／レプリカ数の関係を読み、意図通りに評価されているかを確認するため。
- メトリクス取得の健全性確認（例：`kubectl top pod`）
  → 使用状況が実際に取得できているかを確かめ、「基準にする指標が本当に測れているか」を裏づけるため。

> いずれも「前提を確認する → 使える形を知る → 設定後に評価される様子を読む」という**目的の連鎖**で教える。特定のオプションを覚えることが本質ではない。

---

## 5. 修正後に確認すること

「作成できた」ではなく「意図通りに機能する状態になった」かを、記録で確認させる。

- スケール設定が対象ワークロードを正しく参照しているか（`kubectl get hpa` / `describe hpa` で参照先を確認）。
- 現在のメトリクス値が取得できているか（`<unknown>` のままなら、メトリクス取得や request の前提を疑う）。
- 最小・最大の範囲が意図通りに設定され、現在のレプリカ数がその範囲内にあるか。
- （可能なら）負荷を与えたときにレプリカが増える方向へ動くか、負荷が引いたときに設計した性質で縮小するか。

> 「現在値が取れているか」を最重要の確認点として強調する。ここが取れていないと、範囲や挙動を正しく設定しても機能しない。

---

## 6. 学習ポイント

- **HPA の構造**：HPA は「対象・基準・範囲・挙動」という4つの要素で成り立つ。この分解で理解すると、どんなスケーリング要件も同じ枠組みで整理できる。
- **割合という考え方**：CPU 使用率は「絶対値」ではなく「request に対する割合」。だからスケーリングは request 設計と切り離せない。リソース設計とスケーリング設計はつながっている。
- **増やすと減らすは非対称**：不足への対応（増加）は速さが、過剰への対応（減少）は安定性が重視される。急な縮小が稼働中の処理へ与える影響を、挙動の観点として設計する。
- **前提を疑う運用習慣**：「設定したのに動かない」ときは、値の間違いより先に「メトリクスが取れているか」「request があるか」という前提を確認する。
- **設計意図を語れること**：最小・最大・基準・縮小の性質を「なぜそうしたか」で説明できることが、運用で引き継げる資産になる。

> ねらいは「この問題の答え」を覚えさせることではなく、**初めて見るスケーリング要件でも、対象→基準→範囲→挙動の順で設計を組み立てられる型**を身につけさせること。
